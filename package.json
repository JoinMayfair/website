{
    "name": "mayfair-website",
    "type": "module",
    "version": "0.0.1",
    "scripts": {
      "dev": "astro dev",
      "start": "astro dev",
      "build": "astro build",
      "preview": "astro preview",
      "check": "astro build && tsc && wrangler deploy --dry-run",
      "deploy": "astro build && wrangler deploy",
      "preview": "astro build && wrangler dev",
      "types": "wrangler types"
    },
    "dependencies": {
      "astro": "^3.0.0",
      "@astrojs/tailwind": "^5.0.0",
      "tailwindcss": "^3.3.3"
    },
    "devDependencies": {
      "wrangler": "4.7.0"
    }
  }
